# soundscape-manager

Lightweight soundscape manager (scaffold). This directory contains a small Python project skeleton matching the architecture plan in the repository `docs/architecture_plan.txt`.

See full architecture notes at: ../docs/architecture_plan.txt

Purpose: quick starter files and placeholders for `src/`, `config/`, `assets/`, and `tests/`.
